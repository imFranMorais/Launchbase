            <div class="item">
                <div id=title>Imagem da receita</div>
                <div>
                    <input 
                        type="url" 
                        name="image" 
                        placeholder="http://"
                        value="{{ recipe.image }}"
                    >
                </div>
            </div>

            <div class="item">
                <div id=title>Chef</div>
                <div>
                    <select name="chef">
                        {% for chef in chefOptions %}
                        <option value="{{chef.id}}"
                            {% if recipe.chef_id == "chef.id"%}selected{%endif%}>
                                {{chef.name}}
                        </option>
                        {% endfor %}
                    </select>
                </div>
            </div>

            <div class="item">
                <div id=title>Nome da receita</div>
                <div>
                    <input 
                        type="text" 
                        name="name" 
                        placeholder="Nome da receita"
                        value="{{ recipe.name }}"
                    >
                </div>
            </div>

            <div class="item">
                <div id=title>Ingredientes</div>
                <div id="ingredients">
                {% if recipe.ingredients.length > 0 %}
                    {% for ingredient in recipe.ingredients %}
                        <div class="ingredient">
                            <input  type="text" name="ingredients[]" placeholder="Ingrediente" value="{{ ingredient }}">
                        </div>
                    {% endfor %}
                {% else %}
                        <div class="ingredient">
                            <input  type="text" name="ingredients[]" placeholder="Ingrediente">
                        </div>
                {% endif %}
                </div>
                <button type="button" class="add-input" name="ingredient">Adicionar novo ingrediente</button>
            </div>
            

            <div class="item">
                <div id=title>Modo de preparo</div>
                <div id="preparation">
                {% if recipe.preparation.length > 0 %}
                    {% for step in recipe.preparation %}
                    <div class="step">
                        <input 
                            type="text" 
                            name="preparation[]"
                            placeholder="Passo"
                            value="{{ step }}"
                        >
                    </div>
                {% endfor %}
                {% else %}
                <div class="step">
                        <input 
                            type="text" 
                            name="preparation[]"
                            placeholder="Passo"
                        >
                    </div>
                {% endif    %}
                </div>
                <button type="button" class="add-input" name="step">Adicionar novo passo</button>
            </div>

            <div class="item">
                <div id=title>Informações adicionais</div>
                <div id="information">
                    <textarea name="information">{{ recipe.information }}</textarea>
                </div>
            </div>

    <script src="/scripts.js"></script>
