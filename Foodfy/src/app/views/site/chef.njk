{% extends "site/layout.njk" %}

{% block head %}
    <title>Foodfy | Chef</title>
{% endblock %}

{% block logoImg %}
    <div class="logo-image">
        <a href="/site/index"><img src="/image/logo.png" alt="logo foodfy"></a>
    </div>
{% endblock %}


{% block content %}

    <div id="chef.name">
        <h2>Chef: {{chef.name}}</h2>
    </div>

    <div id="chef_image">
        <img src="{{chef.image}}" alt="">
    </div>

    <div id="chef.name">
        <h1>{{chef.name}}</h1>
        <p>{{chef.total_recipes}} receitas</p>
    </div>

    <div class="wrapper">
        <h2>Receitas</h2>
    </div>

    <section class="cards">
        {% for recipe in recipes %}
            <div class="card">
                <div class="card__image-container">
                    <img src="{{recipe.image}}" alt="Imagem de {{recipe.name}}">
                </div>
                <div class="card__content">
                    <p>{{recipe.name}}</p>
                    {# <span>por {{recipe.chef_id}}</span> #}
                    <a href="/recipes/{{recipe.id}}">Visualizar</a>
                </div>
            </div>
        {% endfor %}
    </section>

{% endblock content %}